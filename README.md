# altv-camera-util

```js
import Camera from 'camera'

const myCam = new Camera('DEFAULT_SCRIPTED_CAMERA', {x: 0, y: 0, z: 72}, {x: 0, y: 0, z: 0}, 65)
myCam.setActive(true)
game.renderScriptCams(true, false, 0, true, false)
```